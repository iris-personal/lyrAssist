<%- include('../partials/header') %>

<section id="post">
  <div class="user"><%= post.user.name %>&nbsp;&nbsp;<img alt="avatar" src="<%= post.user.avatar %>" referrerpolicy="no-referrer" ></div>
  <br><br>
  <div class="vpost"><%= post.content %></div>
  <% if (user._id.equals(post.user._id)) { %>
    <a href="/posts/<%= post._id %>/edit" class="update">Update Post</a>
  <% } %>  
</section>

<br><br><h2>Comments</h2>
  <form id="add-comment-form" method="POST"
  action="/comments/<%= post._id %>">
  <label>New Comment:</label>
  <textarea name="content"></textarea>
    <input class="add" type="submit" value="Add Comment">
  </form>

<br><br><table>
    <thead>
      <tr>
        <th>User</th>
        <th>Date</th>
        <th>Comment</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% post.comments.forEach(function(c) { %>
        <tr>
          <td class="user"><%= post.user.name %>&nbsp;&nbsp;<img alt="avatar" src="<%= post.user.avatar %>" referrerpolicy="no-referrer" ></td>
          <td><%= c.createdAt.toLocaleDateString() %></td>
          <td><%= c.content %></td>
          <td>
            <% if (user._id.equals(c.user)) { %>
              <form id="editComment" action="/comments/<%= c._id %>/?_method=PUT" method="POST">
                <label>Update Comment:</label>
                <textarea name="content"><%= c.content %></textarea>
                <br>
                <button type="submit">Update Comment</button>
              </form>
              <form action="/comments/<%= c._id %>?_method=DELETE" method="POST">
                <button type="submit">X</button>
              </form>
            <% } %>
          </td>
        </tr>
      <% }); %>


<%- include('../partials/footer') %>